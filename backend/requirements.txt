# Direct dependencies only. pip resolves the rest, which keeps this working on
# newer Python versions (e.g. 3.14) instead of pinning conflicting transitive deps.
fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.27
python-dotenv==1.2.2
pydantic==2.13.0
supabase==2.28.3
google-genai==1.73.0
elevenlabs==2.43.0
